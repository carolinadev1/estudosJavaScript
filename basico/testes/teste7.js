/*
Declare duas variáveis booleanas e use o operador OR para verificar 
se pelo menos uma delas é verdadeira. Exiba o resultado no console
*/

const cabeloCrespo = true
const cabeloCacheado = false

const cabelos = cabeloCacheado || cabeloCrespo ? console.log("são parecidos!") : console.log("não, são diferentes!")